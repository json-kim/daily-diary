import 'package:flutter/material.dart';

class DefaultSnackBar extends StatelessWidget {
  const DefaultSnackBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
